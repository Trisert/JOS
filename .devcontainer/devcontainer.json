{
  "name": "JOS - RedPill OBSW (STM32L496VGTx + FreeRTOS)",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "postCreateCommand": "sudo bash -c 'apt-get update -qq && apt-get install -y -qq curl ca-certificates openssh-server gcc-arm-none-eabi libnewlib-arm-none-eabi && service ssh start && curl -fsSL https://tailscale.com/install.sh | sh && tailscale up --authkey=\"$TS_AUTH\" --hostname=\"jos-codespace\"' && echo 'Toolchain ready: run make all in JOS/'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools",
        "marus25.cortex-debug"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  "remoteUser": "vscode",
  "hostRequirements": {
    "cpus": 2,
    "memory": "4gb"
  },
  "forwardPorts": [22],
  "containerEnv": {
    "TS_AUTH": "${TS_AUTH}"
  }
}
